# Isolation Runs (One Change vs OG)

Each experiment flips exactly one knob relative to the OG baseline (BF16, BS=16, no quant, no sort). Summary table: `isolation/2026-03-12_21-33-44/experiments_summary.csv`. Per-experiment folders contain run logs and artifacts.
